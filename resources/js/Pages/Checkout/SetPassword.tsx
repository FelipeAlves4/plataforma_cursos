import BrandLogo from '@/Components/BrandLogo';
import { Head, useForm } from '@inertiajs/react';

export default function SetPassword({ action, email }: { action: string; email: string }) {
    const form = useForm({ password: '', password_confirmation: '' });

    return <main className="grid min-h-screen place-items-center bg-[#0C0A0F] px-5 py-10 text-white"><Head title="Criar senha" /><section className="w-full max-w-md rounded-2xl border border-white/10 bg-[#19151F] p-6 shadow-2xl sm:p-8"><BrandLogo className="h-9 w-auto" /><p className="mt-10 text-xs font-black uppercase tracking-[0.2em] text-[#CDA3FF]">Acesso confirmado</p><h1 className="mt-3 text-3xl font-black tracking-[-0.04em]">Crie sua senha</h1><p className="mt-3 text-sm leading-6 text-white/60">Sua conta para <strong className="text-white">{email}</strong> está pronta. Escolha uma senha para acessar seus cursos.</p><form className="mt-7 space-y-4" noValidate onSubmit={(event) => { event.preventDefault(); form.post(action); }}><div><label className="text-sm font-bold" htmlFor="password">Senha</label><input autoComplete="new-password" className="mt-2 w-full rounded-lg border border-white/15 bg-[#100D15] px-4 py-3 text-white outline-none focus:border-[#C082FF] focus:ring-2 focus:ring-[#A74CE7]/35" id="password" onChange={(event) => form.setData('password', event.target.value)} type="password" value={form.data.password} />{form.errors.password && <p className="mt-1.5 text-sm text-rose-300" role="alert">{form.errors.password}</p>}</div><div><label className="text-sm font-bold" htmlFor="password_confirmation">Confirmar senha</label><input autoComplete="new-password" className="mt-2 w-full rounded-lg border border-white/15 bg-[#100D15] px-4 py-3 text-white outline-none focus:border-[#C082FF] focus:ring-2 focus:ring-[#A74CE7]/35" id="password_confirmation" onChange={(event) => form.setData('password_confirmation', event.target.value)} type="password" value={form.data.password_confirmation} /></div><button className="flex min-h-12 w-full items-center justify-center rounded-lg bg-[#9C48DC] px-5 py-3 text-sm font-extrabold transition hover:bg-[#AD5BEC] disabled:opacity-60" disabled={form.processing} type="submit">{form.processing ? 'Salvando…' : 'Criar senha e acessar'}</button></form></section></main>;
}
