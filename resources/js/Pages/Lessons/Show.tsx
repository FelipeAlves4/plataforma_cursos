import VideoPlayer from '@/Components/VideoPlayer';
import AppLayout from '@/Layouts/AppLayout';
import { Head, Link, router } from '@inertiajs/react';
type Props = { lesson: { id: number; title: string; description?: string | null; videoProvider: 'youtube' | 'panda'; videoId?: string | null; videoUrl?: string | null; completed: boolean }; course: { title: string; slug: string; modules: Array<{ id: number; title: string; lessons: Array<{ id: number; title: string; completed: boolean }> }> }; navigation: { previousLessonId?: number | null; nextLessonId?: number | null } };
export default function Show({ lesson, course, navigation }: Props) { const update = (completed: boolean, next?: number | null) => router.put(`/lessons/${lesson.id}/progress`, { completed }, { preserveScroll: true, onSuccess: () => { if (next) router.visit(`/lessons/${next}`); } }); const next = () => navigation.nextLessonId && update(true, navigation.nextLessonId); return <AppLayout><Head title={lesson.title} /><div className="grid gap-8 xl:grid-cols-[minmax(0,1fr)_22rem]"><section><VideoPlayer provider={lesson.videoProvider} videoId={lesson.videoId} videoUrl={lesson.videoUrl} title={lesson.title} /><div className="mt-7 flex flex-wrap items-start justify-between gap-4"><div><Link href={`/courses/${course.slug}`} className="text-sm font-semibold text-brand-700">{course.title}</Link><h1 className="mt-2 text-3xl font-black tracking-tight">{lesson.title}</h1></div><button type="button" onClick={() => update(!lesson.completed)} className={`rounded-lg px-4 py-2.5 text-sm font-bold ${lesson.completed ? 'bg-brand-100 text-brand-700' : 'bg-ink text-white hover:bg-brand-700'}`}>{lesson.completed ? '✓ Aula concluída' : 'Marcar como concluída'}</button></div><p className="mt-5 max-w-3xl whitespace-pre-line leading-7 text-ink/65">{lesson.description}</p><nav aria-label="Navegação entre aulas" className="mt-9 flex justify-between gap-4 border-t border-ink/10 pt-6"><Link className={`rounded-lg border border-ink/15 px-4 py-2 font-semibold ${!navigation.previousLessonId ? 'pointer-events-none opacity-40' : ''}`} href={navigation.previousLessonId ? `/lessons/${navigation.previousLessonId}` : '#'}>← Aula anterior</Link><button type="button" disabled={!navigation.nextLessonId} onClick={next} className="rounded-lg bg-brand-500 px-4 py-2 font-bold text-ink disabled:opacity-40">Próxima aula →</button></nav></section><aside className="overflow-hidden rounded-2xl border border-ink/10 bg-white xl:sticky xl:top-8 xl:h-[calc(100vh-8rem)]"><div className="border-b border-ink/10 px-5 py-4"><p className="eyebrow text-brand-700">Conteúdo do curso</p><h2 className="mt-1 font-bold">{course.title}</h2></div><div className="max-h-[60vh] overflow-y-auto p-3 xl:max-h-[calc(100vh-14rem)]">{course.modules.map(module => <section key={module.id} className="mb-4"><h3 className="px-2 py-2 text-sm font-bold">{module.title}</h3>{module.lessons.map(courseLesson => <Link key={courseLesson.id} href={`/lessons/${courseLesson.id}`} className={`mb-1 flex items-center gap-3 rounded-lg px-3 py-2 text-sm ${courseLesson.id === lesson.id ? 'bg-brand-100 font-bold text-ink' : 'text-ink/65 hover:bg-sand'}`}><span>{courseLesson.completed ? '✓' : courseLesson.id === lesson.id ? '▶' : '○'}</span><span>{courseLesson.title}</span></Link>)}</section>)}</div></aside></div></AppLayout>; }
